# Known Bugs

No TODO/FIXME markers found in `packages/*/src/**/*.ts` at time of writing.

- None identified.

Recently resolved on this branch:

- `fix(desktop): load root .env via dotenv + move dev port to 5273`
- `fix(core): update test expectations for investment-form Kelly criterion`
- `security: remove hardcoded API keys from source — load from env vars`
- `fix(desktop): Electron native module resolution + better-sqlite3 rebuild`

Report new issues against the `dhando-analyzer` repo (this is a worktree of it).
